# 增强版库存优化提示词模板（codex-console 版）

```text
请继续优化当前这套 `codex-console + CLIProxyAPI + openClaw` 部署，目标如下：

1. 提高 codex-console 账号维护与补充成功率。
2. 提高自动补货稳定性。
3. 保证 CLIProxyAPI 的有效库存长期维持在至少 <替换为你的目标库存数量> 个账号以上。
4. 如果当前代码里存在导致只告警不补货、0 库存不触发补货、随机失败率偏高的问题，请直接修复代码并部署生效。
5. 请不要只改配置，还要检查调度逻辑、失败重试、健康检查和同步链路。
6. 最后告诉我：
   - 你改了哪些代码
   - 你调整了哪些参数
   - 当前库存是多少
   - 还有哪些残余风险
```
